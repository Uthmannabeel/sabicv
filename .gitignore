# ECC harness — local dev aid, not a build artifact
.claude/

# Secrets
.env
.env.local
.env.*.local

# Customer order data (file-backed store) — never commit
data/

# Dependencies & build output
node_modules/
dist/
.next/
out/
coverage/

# OS / editor noise
.DS_Store
Thumbs.db

# Test artifacts
test-fixtures/*.pdf
test-fixtures/docx-stage/
